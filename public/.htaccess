<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /cc/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-l
    RewriteRule . /cc/index.html [L]
</IfModule>